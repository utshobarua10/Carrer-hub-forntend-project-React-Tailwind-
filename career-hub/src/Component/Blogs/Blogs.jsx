import React from 'react';
import { Outlet } from 'react-router-dom';

const HomeTest = () => {
    return (
        <div>
            <p>What is context Api</p>
            <p>The context api is an api who stores all the data to a single place and helps to distribute the data to its child components</p>
        </div>
    );
};

export default HomeTest;