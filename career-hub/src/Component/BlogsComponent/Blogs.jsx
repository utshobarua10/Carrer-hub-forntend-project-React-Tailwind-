import React from 'react';

const Blogs = () => {
    return (
        <div>
            <p>What is context Api</p>
            <p>The context api is an api who stores all the data to a single place and helps to distribute the data to its child components</p>
            <p>What is custom Hook</p>
            <p>Hook is basically something that will trigger when a event occurs, so custom hook is basically something that to trigger the function when some actions occurs</p>
        </div>
    );
};

export default Blogs;